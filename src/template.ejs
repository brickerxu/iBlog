<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <% 
            var css = htmlWebpackPlugin.files.css;
            for (var i = 0; i < css.length; i++) { %>
                <link rel="stylesheet" href="<%=css[i]%>" />
        <% } %>
    </head>

    <body>
        <div id="app"></div>
        <% 
            var js = htmlWebpackPlugin.files.js;
            for (var i = 0; i < js.length; i++) { %>
                <script src="<%=js[i]%>"></script>
        <% } %>
    </body>
</html>